This is a poc of small infrastructure running webrtc components.

# Run
```
docker-compose build
docker-compose up
```
# Signaling Server
To test signaling server is up go to http://localhost:9000/myapp